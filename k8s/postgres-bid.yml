apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgdata-bid
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-bid
  labels:
    app: postgres-bid
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-bid
  template:
    metadata:
      labels:
        app: postgres-bid
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: biddb
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
      volumes:
        - name: pgdata
          persistentVolumeClaim:
            claimName: pgdata-bid
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-bid
spec:
  selector:
    app: postgres-bid
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
